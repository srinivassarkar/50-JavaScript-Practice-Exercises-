// Create a function to calculate the area of a rectangle given its width and height.
//Area of a rectangle = length × width .

function areaOfReactangle(len, wid) {
  return len * wid;
}

console.log(areaOfReactangle(10, 10));
